package gui;

import java.util.ArrayList;

import javafx.geometry.Pos;
import javafx.scene.SubScene;
import javafx.scene.control.Button;
import javafx.scene.control.ContentDisplay;
import javafx.scene.control.Tooltip;
import javafx.scene.layout.VBox;
import model.abilities.Ability;
import model.world.Champion;
 

public class ChampCards{
	
	
	/*public ChampCards(Champion c, String text) {
		
        setText(text);
		setButtonFont();
		setPrefWidth(190);
		setPrefHeight(49);
	}
	//private Button getButton(Champion champion, String name) {
		//   Button button = new Button("Choose the " + name + ".", new ImageView(champion.portrait()));
		//    button.setTooltip(new Tooltip(champion.description()));
		//    button.setContentDisplay(ContentDisplay.TOP);
		//    return button;
	//	}

		private VBox getBox(Button button) {
		    VBox layout = new VBox(10);
		    layout.setAlignment(Pos.CENTER);
		    layout.getChildren().add(button);
		    return layout;
		}
		private String ChampDetails(Champion c) {
			String r="Name:"+ c.getName()+'\n'
					+"Health Points: "+c.getMaxHP()+'\n'
					+ "Actions Points Per Turn: "+c.getMaxActionPointsPerTurn()+'\n'
					+"Mana: "+c.getMana()+'\n'
					+"Attack Range: "+c.getAttackRange()+'\n'
					+"Attack Damage: "+c.getAttackDamage()+'\n'+"Champions's Abilities: ";
			for(int i=0 ; i<c.getAbilities().size();i++) {
				r=r+c.getAbilities().get(i).getName();
				
			}
			return r;
			
			
		}*/
		
		
		
	

}
